---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/service/role_test.go
  - internal/service/patient_test.go
  - internal/service/registry_test.go
  - internal/api/middleware/auth_test.go
  - internal/api/handlers/patients_test.go
  - internal/testutil/mocks.go
  - internal/testutil/doc.go
  - Makefile
autonomous: true
requirements:
  - META-01

must_haves:
  truths:
    - "Running go test ./internal/... -short produces test output (even if tests are skipped or stubbed)"
    - "Mock implementations exist for all repository interfaces used in Phase 1"
    - "Makefile has a test target that runs the full test suite"
  artifacts:
    - path: "internal/testutil/mocks.go"
      provides: "Mock repository implementations for RoleRepository, PatientRepository, RegistryRepository"
    - path: "internal/service/role_test.go"
      provides: "Test stubs for RoleService (RBAC-01)"
    - path: "internal/service/patient_test.go"
      provides: "Test stubs for PatientService (PAT-01, PAT-02, PAT-03, PAT-04, META-01, META-03)"
    - path: "internal/service/registry_test.go"
      provides: "Test stubs for RegistryService (REG-04)"
    - path: "internal/api/middleware/auth_test.go"
      provides: "Test stubs for RequireRole middleware (RBAC-01, RBAC-04)"
    - path: "internal/api/handlers/patients_test.go"
      provides: "Test stubs for patient handler access control (RBAC-02, RBAC-03)"
    - path: "Makefile"
      provides: "test target"
      contains: "go test"
  key_links: []
---

<objective>
Create the Wave 0 test infrastructure: mock repository implementations, test file stubs for all Phase 1 services and middleware, and a Makefile test target.

Purpose: VALIDATION.md requires 7 test files and mock implementations before any plan can run behavioral tests in its <verify> section. This plan creates those scaffolds so that subsequent plans (01-01 through 01-04) can run `go test` commands as part of their verification.

Output: Mock repository implementations in internal/testutil/, test stubs in service and middleware packages, Makefile test target.
</objective>

<execution_context>
@C:/Users/adoma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adoma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock repositories and test utility package</name>
  <files>
    internal/testutil/doc.go,
    internal/testutil/mocks.go
  </files>
  <action>
    Create a testutil package with mock repository implementations that subsequent plans' tests will use.

    1. **Create `internal/testutil/doc.go`:** Package doc comment: "Package testutil provides test doubles and utilities for unit testing."

    2. **Create `internal/testutil/mocks.go`:**
       - Define `MockRoleRepository` struct with function fields matching the RoleRepository interface:
         - `UpdateUserRoleFn func(ctx context.Context, userID int64, role string) error`
         - `GetUserRoleFn func(ctx context.Context, userID int64) (string, error)`
         - `CountUsersFn func(ctx context.Context) (int64, error)`
       - Each method delegates to its function field, returning zero values if the field is nil.

       - Define `MockPatientRepository` struct with function fields matching PatientRepository interface:
         - `CreateFn`, `GetByIDFn`, `UpdateFn`, `ListFn`, `GetSessionHistoryFn`
       - Same delegation pattern.

       - Define `MockRegistryRepository` struct with function fields matching RegistryRepository interface:
         - `ListDevicesFn`, `GetDeviceByIDFn`, `ListProductsFn`, `GetProductByIDFn`, `ListIndicationCodesFn`, `ListClinicalEndpointsFn`
       - Same delegation pattern.

       NOTE: These mocks reference interfaces from the service package. Since the service package does not exist yet (created in Plan 01-01), this file will initially reference interfaces that do not yet exist. The file structure and patterns are established here. Plan 01-01 will create the actual interfaces and this file will compile after 01-01 completes. If the executor encounters compile errors due to missing service interfaces, that is expected -- the test stubs use build tag `//go:build integration` or are simply stubs that will compile once Plan 01-01 is done.

       **Alternative approach (preferred):** Since the repository interfaces do not exist yet, create the mock file with a `//go:build ignore` build tag at the top so it does not fail `go build`. Plan 01-01 will remove this tag after creating the interfaces. Include a comment: "Remove //go:build ignore after Plan 01-01 creates service interfaces."

    **Lint compliance:** All comments end with period. No naked returns. Explicitly handle nil function fields.
  </action>
  <verify>
    <automated>cd C:/Repos/dermify-api && go build ./internal/testutil/... 2>&1 || echo "Expected: mocks use //go:build ignore until service interfaces exist"</automated>
  </verify>
  <done>testutil package exists with mock structs for all three repository interfaces. File uses //go:build ignore tag until service interfaces are created in Plan 01-01.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs and Makefile test target</name>
  <files>
    internal/service/role_test.go,
    internal/service/patient_test.go,
    internal/service/registry_test.go,
    internal/api/middleware/auth_test.go,
    internal/api/handlers/patients_test.go,
    Makefile
  </files>
  <action>
    Create test file stubs and the Makefile test target.

    1. **Create `internal/service/role_test.go`:**
       - Package: `service_test`
       - Use `//go:build ignore` tag (service package does not exist yet).
       - Import `testing` and `github.com/stretchr/testify/assert`.
       - Add stub functions:
         - `TestAssignRole_ValidRole(t *testing.T)` -- stub with `t.Skip("Implemented in Plan 01-01")`
         - `TestAssignRole_InvalidRole(t *testing.T)` -- stub
         - `TestIsFirstUser(t *testing.T)` -- stub
         - `TestGetUserRole(t *testing.T)` -- stub
       - Suppress unused import lint with `var _ = assert.Equal`.

    2. **Create `internal/service/patient_test.go`:**
       - Package: `service_test`
       - Use `//go:build ignore` tag.
       - Stub functions:
         - `TestCreatePatient_ValidData(t *testing.T)` -- stub
         - `TestCreatePatient_MissingFields(t *testing.T)` -- stub
         - `TestListPatients_DefaultPagination(t *testing.T)` -- stub
         - `TestListPatients_CustomPagination(t *testing.T)` -- stub
         - `TestUpdatePatient_VersionConflict(t *testing.T)` -- stub
         - `TestGetPatientSessions_EmptyList(t *testing.T)` -- stub
         - `TestMetadataTracking(t *testing.T)` -- stub covering META-01
         - `TestVersionIncrement(t *testing.T)` -- stub covering META-03

    3. **Create `internal/service/registry_test.go`:**
       - Package: `service_test`
       - Use `//go:build ignore` tag.
       - Stub functions:
         - `TestListDevices_All(t *testing.T)` -- stub
         - `TestListDevices_ByType(t *testing.T)` -- stub
         - `TestListProducts_All(t *testing.T)` -- stub
         - `TestGetDeviceByID_NotFound(t *testing.T)` -- stub

    4. **Create `internal/api/middleware/auth_test.go`:**
       - Package: `middleware_test`
       - Use `//go:build ignore` tag.
       - Stub functions:
         - `TestRequireRole_AllowedRole(t *testing.T)` -- stub
         - `TestRequireRole_DeniedRole(t *testing.T)` -- stub
         - `TestRequireRole_NoRole(t *testing.T)` -- stub
         - `TestRequireRole_NoClaims(t *testing.T)` -- stub

    5. **Create `internal/api/handlers/patients_test.go`:**
       - Package: `handlers_test`
       - Use `//go:build ignore` tag.
       - Stub functions:
         - `TestDoctorAccess_PatientsEndpoint(t *testing.T)` -- stub for RBAC-02
         - `TestAdminAccess_PatientsEndpoint(t *testing.T)` -- stub for RBAC-03
         - `TestUnauthorizedAccess_PatientsEndpoint(t *testing.T)` -- stub

    6. **Modify `Makefile`:**
       - Add a `test` target after the existing `lint-fix` target:
         ```
         test:
         	@go test ./... -count=1 -v
         ```
       - Add a `test-short` target:
         ```
         test-short:
         	@go test ./internal/... -count=1 -short
         ```

    All test files use `//go:build ignore` initially. Each subsequent plan (01-01 through 01-04) removes the build tag from the relevant test files and implements the actual test logic, replacing `t.Skip()` with real assertions.
  </action>
  <verify>
    <automated>cd C:/Repos/dermify-api && make test 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 test files exist with stub functions matching VALIDATION.md requirements. Makefile has `test` and `test-short` targets. Test stubs use //go:build ignore until their corresponding implementation plans complete. `make test` runs without errors (ignored files are excluded).</done>
</task>

</tasks>

<verification>
1. All 5 test files exist in the correct packages.
2. Mock file exists at internal/testutil/mocks.go.
3. Makefile has `test` and `test-short` targets.
4. `make test` runs without failure (build-ignored files excluded from compilation).
5. Each test stub function name matches the VALIDATION.md Per-Task Verification Map commands.
</verification>

<success_criteria>
- Test infrastructure files exist for all VALIDATION.md Wave 0 requirements.
- Makefile `test` and `test-short` targets are functional.
- Mock repository structs are scaffolded for RoleRepository, PatientRepository, RegistryRepository.
- All subsequent plans can reference these test files in their <verify> sections.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>
