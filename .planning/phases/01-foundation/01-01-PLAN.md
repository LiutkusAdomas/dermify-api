---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - internal/domain/role.go
  - internal/domain/patient.go
  - internal/domain/device.go
  - internal/domain/product.go
  - internal/domain/registry.go
  - internal/domain/doc.go
  - internal/service/doc.go
  - internal/service/role.go
  - internal/repository/postgres/doc.go
  - internal/repository/postgres/role.go
autonomous: true
requirements:
  - RBAC-01

must_haves:
  truths:
    - "Domain model types compile and are importable within internal packages"
    - "RoleService validates roles and delegates to RoleRepository"
    - "PostgresRoleRepository implements all RoleRepository interface methods"
    - "Service/repository architecture pattern is established for all subsequent plans to follow"
  artifacts:
    - path: "internal/domain/role.go"
      provides: "Role constants (RoleDoctor, RoleAdmin) and validation"
    - path: "internal/domain/patient.go"
      provides: "Patient domain model with metadata fields"
    - path: "internal/domain/device.go"
      provides: "Device and Handpiece domain models"
    - path: "internal/domain/product.go"
      provides: "Product domain model"
    - path: "internal/domain/registry.go"
      provides: "IndicationCode and ClinicalEndpoint domain models"
    - path: "internal/service/role.go"
      provides: "RoleService with RoleRepository interface"
      exports: ["RoleService", "RoleRepository", "NewRoleService"]
    - path: "internal/repository/postgres/role.go"
      provides: "PostgreSQL RoleRepository implementation"
      exports: ["PostgresRoleRepository", "NewPostgresRoleRepository"]
  key_links:
    - from: "internal/service/role.go"
      to: "internal/repository/postgres/role.go"
      via: "RoleRepository interface"
      pattern: "repo\\.UpdateUserRole"
---

<objective>
Create domain models for all Phase 1 entities and establish the service/repository architecture with the RoleService as the first implementation.

Purpose: This plan creates the foundational layered architecture (domain -> service -> repository) that all subsequent plans build on. The domain models define the data shapes for roles, patients, devices, products, and registry entities. The RoleService + PostgresRoleRepository are the first concrete service/repository pair, establishing the pattern.

Output: Domain model types for all Phase 1 entities, RoleService with RoleRepository interface, PostgresRoleRepository implementation.
</objective>

<execution_context>
@C:/Users/adoma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adoma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From migrations/20251213115406_users_table.sql:
```sql
CREATE TABLE IF NOT EXISTS users (
  id BIGSERIAL PRIMARY KEY,
  username VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  bio TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain models</name>
  <files>
    internal/domain/doc.go,
    internal/domain/role.go,
    internal/domain/patient.go,
    internal/domain/device.go,
    internal/domain/product.go,
    internal/domain/registry.go
  </files>
  <action>
    Create the domain package with all Phase 1 entity types.

    1. **Create `internal/domain/doc.go`:** Package doc comment for domain package.

    2. **Create `internal/domain/role.go`:**
       - Define role string constants: `RoleDoctor = "doctor"`, `RoleAdmin = "admin"`.
       - Define a `ValidRole(role string) bool` function that checks against the two valid roles.
       - Define `AllRoles() []string` returning both roles.

    3. **Create `internal/domain/patient.go`:**
       - Define `Patient` struct with fields: `ID int64`, `FirstName string`, `LastName string`, `DateOfBirth time.Time`, `Sex string` (male/female/other), `Phone *string` (nullable), `Email *string` (nullable), `ExternalReference *string` (nullable), `Version int`, `CreatedAt time.Time`, `CreatedBy int64`, `UpdatedAt time.Time`, `UpdatedBy int64`.
       - Define `SessionSummary` struct with fields: `ID int64`, `CreatedAt time.Time`, `Status string`. This is a placeholder for Phase 2.
       - All fields use `json` struct tags with snake_case naming.

    4. **Create `internal/domain/device.go`:**
       - Define `Device` struct: `ID int64`, `Name string`, `Manufacturer string`, `Model string`, `DeviceType string` (ipl/ndyag/co2/rf), `Active bool`, `CreatedAt time.Time`, `Handpieces []Handpiece` (for detail views).
       - Define `Handpiece` struct: `ID int64`, `DeviceID int64`, `Name string`, `Active bool`, `CreatedAt time.Time`.

    5. **Create `internal/domain/product.go`:**
       - Define `Product` struct: `ID int64`, `Name string`, `Manufacturer string`, `ProductType string` (filler/botulinum_toxin), `Concentration *string` (nullable), `Active bool`, `CreatedAt time.Time`.

    6. **Create `internal/domain/registry.go`:**
       - Define `IndicationCode` struct: `ID int64`, `Code string`, `Name string`, `ModuleType string`, `Active bool`.
       - Define `ClinicalEndpoint` struct: `ID int64`, `Code string`, `Name string`, `ModuleType string`, `Active bool`.

    **Lint compliance:** No package-level variables (use constants). End all comments with a period. No naked returns.
  </action>
  <verify>
    <automated>cd C:/Repos/dermify-api && go build ./internal/domain/...</automated>
  </verify>
  <done>All domain model files compile. Six entity types defined: Role constants, Patient, Device, Handpiece, Product, IndicationCode, ClinicalEndpoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create service/repository scaffold with RoleService</name>
  <files>
    internal/service/doc.go,
    internal/service/role.go,
    internal/repository/postgres/doc.go,
    internal/repository/postgres/role.go,
    internal/service/role_test.go,
    internal/testutil/mocks.go
  </files>
  <behavior>
    - TestAssignRole_ValidRole: AssignRole with "doctor" calls repo.UpdateUserRole and returns nil
    - TestAssignRole_InvalidRole: AssignRole with "superuser" returns error without calling repo
    - TestIsFirstUser: CountUsers returning 1 produces true, CountUsers returning 5 produces false
    - TestGetUserRole: Delegates to repo and returns the role string
  </behavior>
  <action>
    Create the service and repository packages, implement RoleService and PostgresRoleRepository, then activate and implement the role test stubs from Plan 01-00.

    1. **Create `internal/service/doc.go`:** Package doc comment.

    2. **Create `internal/service/role.go`:**
       - Define `RoleRepository` interface with methods:
         - `UpdateUserRole(ctx context.Context, userID int64, role string) error`
         - `GetUserRole(ctx context.Context, userID int64) (string, error)` -- returns empty string if no role.
         - `CountUsers(ctx context.Context) (int64, error)` -- for first-user bootstrap.
       - Define `RoleService` struct with `repo RoleRepository` field.
       - Define `NewRoleService(repo RoleRepository) *RoleService`.
       - Implement `AssignRole(ctx context.Context, userID int64, role string) error`:
         - Validate role using `domain.ValidRole()`. Return error if invalid.
         - Call `repo.UpdateUserRole()`.
       - Implement `GetUserRole(ctx context.Context, userID int64) (string, error)`.
       - Implement `IsFirstUser(ctx context.Context) (bool, error)` that calls `repo.CountUsers()` and returns true if count <= 1.

    3. **Create `internal/repository/postgres/doc.go`:** Package doc comment.

    4. **Create `internal/repository/postgres/role.go`:**
       - Define `PostgresRoleRepository` struct with `db *sql.DB` field.
       - Define `NewPostgresRoleRepository(db *sql.DB) *PostgresRoleRepository`.
       - Implement all `RoleRepository` interface methods using parameterized SQL:
         - `UpdateUserRole`: `UPDATE users SET role = $1 WHERE id = $2`. Return error if no rows affected (user not found).
         - `GetUserRole`: `SELECT COALESCE(role, '') FROM users WHERE id = $1`.
         - `CountUsers`: `SELECT COUNT(*) FROM users`.

    5. **Activate test stubs in `internal/service/role_test.go`:**
       - Remove the `//go:build ignore` tag.
       - Implement all four test functions using the MockRoleRepository from testutil.
       - Follow RED-GREEN: write test expectations first, then verify they pass against the RoleService implementation.

    6. **Update `internal/testutil/mocks.go`:**
       - Remove the `//go:build ignore` tag.
       - Update MockRoleRepository to import and implement the actual `service.RoleRepository` interface.
       - Leave MockPatientRepository and MockRegistryRepository with `//go:build ignore` or as separate mock files -- they will be activated in Plans 01-03 and 01-04.

       **Alternative approach:** If having both ignore-tagged and non-ignore-tagged content in one file is problematic, split mocks into `mock_role.go`, `mock_patient.go`, `mock_registry.go` with build tags on the latter two.

    **Lint compliance:** Keep functions under 100 lines. No naked returns. No named returns. Explicitly handle all errors. Comments end with period.
  </action>
  <verify>
    <automated>cd C:/Repos/dermify-api && go build ./internal/service/... ./internal/repository/... && go test ./internal/service/ -run TestAssignRole -v -count=1 && go test ./internal/service/ -run TestIsFirstUser -v -count=1</automated>
  </verify>
  <done>Service/repository architecture is established. RoleService and PostgresRoleRepository compile. RoleService unit tests pass: valid role assignment, invalid role rejection, first-user detection, role retrieval.</done>
</task>

</tasks>

<verification>
1. `go build ./internal/domain/... ./internal/service/... ./internal/repository/...` compiles all new packages.
2. `go test ./internal/service/ -run TestAssignRole -v` passes.
3. `go test ./internal/service/ -run TestIsFirstUser -v` passes.
4. Domain models define all entity types for Phase 1.
5. Service/repository pattern established: interface in service package, implementation in repository/postgres package.
</verification>

<success_criteria>
- All domain types compile and are importable within internal packages.
- Service/repository architecture is established with clear interface boundaries.
- RoleService unit tests pass covering valid/invalid role assignment and first-user detection.
- `go build ./internal/...` passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
