---
phase: 05-sign-off-and-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/domain/addendum.go
  - internal/domain/audit.go
  - migrations/20260308040000_add_session_signoff_columns.sql
  - migrations/20260308040001_create_session_addendums.sql
  - migrations/20260308040002_create_audit_trail.sql
  - migrations/20260308040003_create_immutability_triggers.sql
  - migrations/20260308040004_create_audit_triggers.sql
  - internal/service/signoff.go
  - internal/service/addendum.go
  - internal/service/audit.go
  - internal/testutil/mock_addendum.go
  - internal/testutil/mock_audit.go
autonomous: true
requirements:
  - LOCK-02
  - LOCK-03
  - LOCK-04
  - LOCK-05
  - LOCK-06
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - META-02

must_haves:
  truths:
    - "Addendum and AuditEntry domain types exist with all required fields"
    - "Sessions table has signed_at and signed_by columns"
    - "Addendums table exists with FK to sessions and immutability trigger"
    - "Audit trail table exists with append-only protection"
    - "Immutability triggers prevent modification of signed/locked session data across all clinical tables"
    - "Audit triggers capture all INSERT/UPDATE/DELETE on clinical tables"
    - "Service interfaces defined for signoff, addendum, and audit operations"
    - "Mock repositories exist for addendum and audit domains"
  artifacts:
    - path: "internal/domain/addendum.go"
      provides: "Addendum domain type"
      contains: "type Addendum struct"
    - path: "internal/domain/audit.go"
      provides: "AuditEntry domain type"
      contains: "type AuditEntry struct"
    - path: "migrations/20260308040000_add_session_signoff_columns.sql"
      provides: "signed_at, signed_by columns on sessions"
      contains: "ALTER TABLE sessions ADD COLUMN signed_at"
    - path: "migrations/20260308040001_create_session_addendums.sql"
      provides: "session_addendums table"
      contains: "CREATE TABLE session_addendums"
    - path: "migrations/20260308040002_create_audit_trail.sql"
      provides: "audit_trail table with append-only trigger"
      contains: "CREATE TABLE audit_trail"
    - path: "migrations/20260308040003_create_immutability_triggers.sql"
      provides: "BEFORE UPDATE/DELETE triggers on all clinical tables"
      contains: "prevent_signed_session_modification"
    - path: "migrations/20260308040004_create_audit_triggers.sql"
      provides: "AFTER INSERT/UPDATE/DELETE triggers on all clinical tables"
      contains: "audit_trigger_function"
    - path: "internal/service/signoff.go"
      provides: "SignoffService interface and sentinel errors"
      contains: "type SignoffService struct"
    - path: "internal/service/addendum.go"
      provides: "AddendumService and AddendumRepository interface"
      contains: "type AddendumRepository interface"
    - path: "internal/service/audit.go"
      provides: "AuditService and AuditRepository interface"
      contains: "type AuditRepository interface"
    - path: "internal/testutil/mock_addendum.go"
      provides: "MockAddendumRepository"
      contains: "type MockAddendumRepository struct"
    - path: "internal/testutil/mock_audit.go"
      provides: "MockAuditRepository"
      contains: "type MockAuditRepository struct"
  key_links:
    - from: "migrations/20260308040003_create_immutability_triggers.sql"
      to: "sessions, session_consents, contraindication_screenings, session_modules, session_outcomes, all detail tables, session_addendums"
      via: "BEFORE UPDATE/DELETE triggers"
      pattern: "prevent_signed_session_modification|prevent_signed_module_detail_modification|prevent_addendum_modification"
    - from: "migrations/20260308040004_create_audit_triggers.sql"
      to: "audit_trail table"
      via: "AFTER INSERT/UPDATE/DELETE triggers on all clinical tables"
      pattern: "audit_trigger_function"
---

<objective>
Domain types, SQL migrations (schema + triggers), service interfaces, and mock repositories for sign-off, addendums, and audit trail.

Purpose: Establish the complete database schema and Go contracts for Phase 5. The five migrations add signed_at/signed_by columns, create the addendums and audit_trail tables, install immutability triggers on all clinical tables, and install audit triggers. Domain types, service scaffolds, and mock repositories define the contracts that Plan 02 implements.

Output: 2 domain types, 5 SQL migrations, 3 service files with interfaces and sentinel errors, 2 mock repository files.
</objective>

<execution_context>
@C:/Users/adoma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adoma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sign-off-and-compliance/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/domain/session.go:
```go
const (
    SessionStatusDraft           = "draft"
    SessionStatusInProgress      = "in_progress"
    SessionStatusAwaitingSignoff = "awaiting_signoff"
    SessionStatusSigned          = "signed"
    SessionStatusLocked          = "locked"
)

type Session struct {
    ID               int64      `json:"id"`
    PatientID        int64      `json:"patient_id"`
    ClinicianID      int64      `json:"clinician_id"`
    Status           string     `json:"status"`
    // ... other fields ...
    Version          int        `json:"version"`
    CreatedAt        time.Time  `json:"created_at"`
    CreatedBy        int64      `json:"created_by"`
    UpdatedAt        time.Time  `json:"updated_at"`
    UpdatedBy        int64      `json:"updated_by"`
}
```

From internal/service/session.go:
```go
type SessionRepository interface {
    Create(ctx context.Context, session *domain.Session) error
    GetByID(ctx context.Context, id int64) (*domain.Session, error)
    Update(ctx context.Context, session *domain.Session) error
    UpdateStatus(ctx context.Context, id int64, status string, expectedVersion int, userID int64) error
    List(ctx context.Context, filter SessionFilter) (*SessionListResult, error)
    ListByPatient(ctx context.Context, patientID int64) ([]domain.SessionSummary, error)
    SetIndicationCodes(ctx context.Context, sessionID int64, codeIDs []int64) error
}

type ModuleRepository interface {
    Create(ctx context.Context, module *domain.SessionModule, userID int64) error
    ListBySession(ctx context.Context, sessionID int64) ([]domain.SessionModule, error)
    Delete(ctx context.Context, id int64, sessionID int64) error
    NextSortOrder(ctx context.Context, sessionID int64) (int, error)
}
```

From internal/service/consent.go:
```go
type ConsentRepository interface {
    ExistsForSession(ctx context.Context, sessionID int64) (bool, error)
}
```

From internal/service/outcome.go:
```go
type OutcomeRepository interface {
    ExistsForSession(ctx context.Context, sessionID int64) (bool, error)
}
```

Existing clinical tables (from migrations):
- sessions (has session_id as `id`)
- session_consents (has session_id FK)
- contraindication_screenings (has session_id FK)
- session_modules (has session_id FK)
- session_outcomes (has session_id FK)
- ipl_module_details (has module_id FK to session_modules)
- ndyag_module_details (has module_id FK)
- co2_module_details (has module_id FK)
- rf_module_details (has module_id FK)
- filler_module_details (has module_id FK)
- botulinum_module_details (has module_id FK)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain types and SQL migrations</name>
  <files>
    internal/domain/addendum.go,
    internal/domain/audit.go,
    migrations/20260308040000_add_session_signoff_columns.sql,
    migrations/20260308040001_create_session_addendums.sql,
    migrations/20260308040002_create_audit_trail.sql,
    migrations/20260308040003_create_immutability_triggers.sql,
    migrations/20260308040004_create_audit_triggers.sql
  </files>
  <action>
    1. Create `internal/domain/addendum.go`:
       - `Addendum` struct with fields: ID (int64), SessionID (int64), AuthorID (int64), Reason (string), Content (string), CreatedAt (time.Time). All JSON-tagged with snake_case.
       - No updated_at, updated_by, or version fields -- addendums are insert-only.

    2. Create `internal/domain/audit.go`:
       - `AuditEntry` struct with fields: ID (int64), Action (string), PerformedAt (time.Time), UserID (*int64 pointer for nullable), EntityType (string), EntityID (int64), OldValues (json.RawMessage with omitempty), NewValues (json.RawMessage with omitempty). All JSON-tagged.
       - Import `encoding/json` for json.RawMessage.

    3. Create migration `20260308040000_add_session_signoff_columns.sql` (META-02):
       - +goose Up: ALTER TABLE sessions ADD COLUMN signed_at TIMESTAMP WITH TIME ZONE; ALTER TABLE sessions ADD COLUMN signed_by BIGINT REFERENCES users(id);
       - +goose Down: DROP COLUMN signed_by, DROP COLUMN signed_at (reverse order).
       - Use `-- +goose StatementBegin` / `-- +goose StatementEnd` wrapping.

    4. Create migration `20260308040001_create_session_addendums.sql` (LOCK-04):
       - CREATE TABLE session_addendums with: id BIGSERIAL PRIMARY KEY, session_id BIGINT NOT NULL REFERENCES sessions(id), author_id BIGINT NOT NULL REFERENCES users(id), reason TEXT NOT NULL, content TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP.
       - CREATE INDEX idx_session_addendums_session_id ON session_addendums(session_id).
       - +goose Down: DROP TABLE IF EXISTS session_addendums.

    5. Create migration `20260308040002_create_audit_trail.sql` (AUDIT-01, AUDIT-02, AUDIT-03):
       - CREATE TABLE audit_trail with: id BIGSERIAL PRIMARY KEY, action VARCHAR(10) NOT NULL CHECK (action IN ('INSERT', 'UPDATE', 'DELETE')), performed_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, user_id BIGINT, entity_type VARCHAR(100) NOT NULL, entity_id BIGINT NOT NULL, old_values JSONB, new_values JSONB.
       - CREATE INDEX idx_audit_trail_entity ON audit_trail(entity_type, entity_id).
       - CREATE INDEX idx_audit_trail_performed_at ON audit_trail(performed_at).
       - CREATE INDEX idx_audit_trail_user_id ON audit_trail(user_id).
       - Create prevent_audit_modification() trigger function that RAISE EXCEPTION 'Audit trail entries cannot be modified or deleted'.
       - CREATE TRIGGER audit_trail_immutable BEFORE UPDATE OR DELETE ON audit_trail FOR EACH ROW EXECUTE FUNCTION prevent_audit_modification().
       - +goose Down: DROP TRIGGER, DROP FUNCTION, DROP TABLE.

    6. Create migration `20260308040003_create_immutability_triggers.sql` (LOCK-03, LOCK-05, LOCK-06):
       - Create function prevent_signed_session_modification() RETURNS TRIGGER: For sessions table, check OLD.status IN ('signed', 'locked'); allow ONLY the signed->locked UPDATE transition; raise exception otherwise. For child tables with session_id column, SELECT status FROM sessions WHERE id = OLD.session_id and raise exception if signed/locked. Handle both UPDATE and DELETE TG_OP.
       - Create function prevent_signed_module_detail_modification() RETURNS TRIGGER: JOIN sessions through session_modules (WHERE sm.id = OLD.module_id) to find session status; raise exception if signed/locked.
       - Create function prevent_addendum_modification() RETURNS TRIGGER: Unconditionally RAISE EXCEPTION 'Addendums cannot be modified or deleted' (LOCK-05).
       - Apply enforce_ prefixed triggers on: sessions, session_consents, contraindication_screenings, session_modules, session_outcomes (using prevent_signed_session_modification). Apply on: ipl_module_details, ndyag_module_details, co2_module_details, rf_module_details, filler_module_details, botulinum_module_details (using prevent_signed_module_detail_modification). Apply on: session_addendums (using prevent_addendum_modification).
       - +goose Down: DROP all triggers, DROP all functions.
       - Follow exact SQL from research doc Pattern 2.

    7. Create migration `20260308040004_create_audit_triggers.sql` (AUDIT-01, AUDIT-04):
       - Create function audit_trigger_function() RETURNS TRIGGER: Extract entity_id from OLD/NEW based on TG_OP. Extract user_id from NEW.created_by (INSERT), NEW.updated_by (UPDATE), OLD.updated_by (DELETE). Use row_to_json(OLD)::JSONB and row_to_json(NEW)::JSONB for old/new values. INSERT INTO audit_trail. RETURN NULL (AFTER trigger).
       - CREATE TRIGGER audit_ prefixed triggers on: sessions, session_consents, contraindication_screenings, session_modules, session_outcomes, ipl_module_details, ndyag_module_details, co2_module_details, rf_module_details, filler_module_details, botulinum_module_details (AFTER INSERT OR UPDATE OR DELETE).
       - For session_addendums: AFTER INSERT ONLY (since addendums are immutable, no UPDATE/DELETE audit needed).
       - +goose Down: DROP all triggers, DROP function.
       - Follow exact SQL from research doc Pattern 3.
  </action>
  <verify>
    <automated>cd C:/Repos/dermify-api && go build ./internal/domain/... 2>&1 | head -20</automated>
  </verify>
  <done>Both domain types compile cleanly. All 5 migration files exist with valid goose Up/Down sections. Addendum has 6 fields (no version/updated_*). AuditEntry has 8 fields including nullable UserID and json.RawMessage old/new values.</done>
</task>

<task type="auto">
  <name>Task 2: Service interfaces, sentinel errors, and mock repositories</name>
  <files>
    internal/service/signoff.go,
    internal/service/addendum.go,
    internal/service/audit.go,
    internal/testutil/mock_addendum.go,
    internal/testutil/mock_audit.go
  </files>
  <action>
    1. Create `internal/service/signoff.go`:
       - Sentinel errors: ErrSessionIncomplete (session is incomplete for sign-off), ErrSessionNotAwaitingSignoff (session not in awaiting_signoff state). Use //nolint:gochecknoglobals // sentinel error pattern.
       - `ValidationResult` struct with Ready bool and Missing []string (both JSON-tagged).
       - `SignoffService` struct with fields: sessionRepo SessionRepository, consentRepo ConsentRepository, moduleRepo ModuleRepository, outcomeRepo OutcomeRepository.
       - `NewSignoffService(sessionRepo, consentRepo, moduleRepo, outcomeRepo)` constructor.
       - `ValidateForSignoff(ctx, sessionID int64) (*ValidationResult, error)` method:
         * Get session by ID; return error if not found.
         * If session.Status != domain.SessionStatusAwaitingSignoff, return ValidationResult{Ready: false, Missing: ["session must be in awaiting_signoff state"]}.
         * Check consent exists via consentRepo.ExistsForSession. If missing, append "consent record" to missing.
         * Check modules exist via moduleRepo.ListBySession. If empty, append "at least one procedure module" to missing.
         * Check outcome exists via outcomeRepo.ExistsForSession. If missing, append "outcome record" to missing.
         * Return ValidationResult{Ready: len(missing)==0, Missing: missing}.
       - `SignOff(ctx, sessionID int64, clinicianID int64) error` method:
         * Call ValidateForSignoff. If error, return it. If not ready, return ErrSessionIncomplete.
         * Call sessionRepo.UpdateStatus(ctx, sessionID, domain.SessionStatusSigned, session.Version, clinicianID) -- but wait, UpdateStatus does not set signed_at/signed_by. So instead: Define a new interface method. Add a `SignoffRepository` interface that extends with a `SignOff(ctx, id int64, clinicianID int64, expectedVersion int) error` method. The SignoffService should use SessionRepository for reads and a dedicated SignOff method.
         * Actually: to keep clean, define a new `SessionSignoffRepository` interface in signoff.go with just: `GetByID(ctx, id int64) (*domain.Session, error)` and `SignOff(ctx, id int64, clinicianID int64, expectedVersion int) error`. This keeps the signoff repo focused. But the simpler approach matching codebase patterns: just add `SignOff` to the SignoffService's own dependency. Use the existing SessionRepository (for GetByID) plus a new `SignoffRepository` interface with just `SignOff(ctx, id, clinicianID, expectedVersion) error`.
         * Final design: SignoffService takes sessionRepo SessionRepository (for GetByID), consentRepo ConsentRepository, moduleRepo ModuleRepository, outcomeRepo OutcomeRepository, and signoffRepo SignoffRepository. The SignoffRepository interface has one method: `SignOff(ctx context.Context, id int64, clinicianID int64, expectedVersion int) error`. This method atomically sets status='signed', signed_at=now, signed_by=clinicianID, increments version, with optimistic locking on expectedVersion. Also add `LockSession(ctx context.Context, id int64, expectedVersion int, userID int64) error` for Signed->Locked transition.
       - Update constructor: `NewSignoffService(sessionRepo SessionRepository, consentRepo ConsentRepository, moduleRepo ModuleRepository, outcomeRepo OutcomeRepository, signoffRepo SignoffRepository)`.
       - `LockSession(ctx, sessionID int64, userID int64) error` method: Get session, verify status is domain.SessionStatusSigned, call signoffRepo.LockSession. Return ErrInvalidStateTransition if not in signed state.

    2. Create `internal/service/addendum.go`:
       - Sentinel errors: ErrAddendumNotFound, ErrInvalidAddendumData, ErrSessionNotLocked (addendums only on locked sessions). Use //nolint pattern.
       - `AddendumRepository` interface with methods:
         * Create(ctx, addendum *domain.Addendum) error
         * GetByID(ctx, id int64) (*domain.Addendum, error)
         * ListBySession(ctx, sessionID int64) ([]domain.Addendum, error)
       - `AddendumService` struct with repo AddendumRepository and sessionRepo SessionRepository.
       - `NewAddendumService(repo AddendumRepository, sessionRepo SessionRepository)` constructor.
       - `CreateAddendum(ctx, addendum *domain.Addendum) error`: Validate reason and content are non-empty, session_id > 0, author_id > 0. Get session, check status == domain.SessionStatusLocked; if not, return ErrSessionNotLocked. Set created_at = time.Now(). Call repo.Create.
       - `GetByID(ctx, id int64) (*domain.Addendum, error)`: delegate to repo.
       - `ListBySession(ctx, sessionID int64) ([]domain.Addendum, error)`: delegate to repo.

    3. Create `internal/service/audit.go`:
       - `AuditFilter` struct with: EntityType string, EntityID int64, UserID int64, Page int, PerPage int.
       - `AuditListResult` struct with: Entries []domain.AuditEntry, Total int.
       - `AuditRepository` interface with methods:
         * ListByEntity(ctx, entityType string, entityID int64) ([]domain.AuditEntry, error)
         * List(ctx, filter AuditFilter) (*AuditListResult, error)
       - `AuditService` struct with repo AuditRepository.
       - `NewAuditService(repo AuditRepository)` constructor.
       - `ListByEntity(ctx, entityType string, entityID int64) ([]domain.AuditEntry, error)`: delegate to repo.
       - `List(ctx, filter AuditFilter) (*AuditListResult, error)`: apply pagination defaults (page=1, perPage=50, max=100) then delegate.
       - Note: audit entries are created by DB triggers, so no Create method in the service. The service is read-only.

    4. Create `internal/testutil/mock_addendum.go`:
       - `MockAddendumRepository` struct with function fields: CreateFn, GetByIDFn, ListBySessionFn -- all typed to match AddendumRepository interface methods.
       - Implement all three methods delegating to their function fields.
       - Use `//go:build ignore` tag at top (matching existing mock pattern).

    5. Create `internal/testutil/mock_audit.go`:
       - `MockAuditRepository` struct with function fields: ListByEntityFn, ListFn -- matching AuditRepository interface.
       - Implement both methods delegating to function fields.
       - Use `//go:build ignore` tag at top.

    6. Also create `internal/testutil/mock_signoff.go`:
       - `MockSignoffRepository` struct with function fields: SignOffFn, LockSessionFn.
       - Implement both methods.
       - Use `//go:build ignore` tag at top.
  </action>
  <verify>
    <automated>cd C:/Repos/dermify-api && go build ./internal/service/... 2>&1 | head -20</automated>
  </verify>
  <done>SignoffService compiles with ValidateForSignoff and SignOff methods. AddendumService compiles with Create/Get/List. AuditService compiles with read-only List methods. All 3 mock files exist with //go:build ignore tags. Sentinel errors follow //nolint:gochecknoglobals pattern.</done>
</task>

</tasks>

<verification>
- `go build ./internal/domain/...` succeeds (domain types compile)
- `go build ./internal/service/...` succeeds (service interfaces compile)
- All 5 migration files exist with valid +goose Up/Down sections
- All 3 mock files exist with //go:build ignore tags
- `go test ./... -count=1` passes (existing tests unbroken)
</verification>

<success_criteria>
- Addendum and AuditEntry domain types defined with correct field types
- 5 SQL migrations create the complete database schema for sign-off, addendums, audit trail, immutability triggers, and audit triggers
- SignoffService, AddendumService, and AuditService have interfaces and sentinel errors defined
- SignoffRepository interface defines SignOff and LockSession methods
- Mock repositories exist for addendum, audit, and signoff domains
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-sign-off-and-compliance/05-01-SUMMARY.md`
</output>
