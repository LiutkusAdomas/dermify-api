# Build output
dermify-api

# Editor
.idea/
.vscode/
*.suo
*.sw?

# Environment / secrets — never commit real credentials
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Local-only DevOps / infra notes (safe to keep locally, never push)
devops-local/

# Test scratch files (e.g. config_test.go previously leaked YAML fixtures here
# on Windows — kept as a safety net even though the test now uses t.TempDir())
config/[0-9]*.yaml
